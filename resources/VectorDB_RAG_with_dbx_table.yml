# A sample job for my_project.

resources:
  jobs:
    VectorDB_RAG_with_dbx_table:
      name: VectorDB_RAG_with_dbx_table

      tasks:
        - task_key: notebook_task
          notebook_task:
            notebook_path: ../src/VectorDB_RAG_with_dbx_table.ipynb

